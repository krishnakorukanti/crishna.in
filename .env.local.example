# crishna.in - Local Development Environment Variables Example
# Copy this file to .env.local and fill in your values
# IMPORTANT: NEVER commit your actual .env.local file to the repository!

# === DATA STORAGE ===
# Redis configuration for development (required for saving conversations)
UPSTASH_REDIS_REST_URL=your_upstash_redis_url
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token

# === AI FEATURES ===
# OpenAI API credentials for the AI assistant functionality
# Get your API key from: https://platform.openai.com/api-keys
# For local development, you can use a lower-tier model if needed
OPENAI_API_KEY=your_openai_api_key
ASSISTANT_ID=your_assistant_id
OPENAI_ORGANIZATION_ID=your_organization_id

# === ANALYTICS ===
# For local development, you can leave these empty or use test credentials
BEAM_TOKEN=your_beam_token

# === GITHUB INTEGRATION ===
# GitHub token for accessing private contributions in the GitHub chart
# SECURITY WARNING: This uses NEXT_PUBLIC_ prefix which means it will be
# exposed to the client. Only use a token with minimal permissions (read:user)
# Create a token at: https://github.com/settings/tokens
# NEXT_PUBLIC_GITHUB_TOKEN=your_github_token

# === DEVELOPMENT SETTINGS ===
# Uncomment these for local development debugging if needed
# DEBUG=true
# NEXT_PUBLIC_SHOW_DEBUG_INFO=true

# Optional: Set your preferred OpenAI model
# OPENAI_MODEL=gpt-4o 